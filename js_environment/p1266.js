/**
 * @param {number[][]} points
 * @return {number}
 */
var minTimeToVisitAllPoints = function(points) {
    let start = points;
    console.log(start[0])
    console.log(start[0][0])
    for(let i = 0 ; i < points.length ; i++){
      while(i!= //TODO: work from here
        if(start[i][0] < points[i+1][1]){
            start[i][0]=start[i][i]+1;
            console.log(start[i]);
            break;
        }
        if(start[i][1] < points[i+1][2]){
        }
    }
};
